# 下载与安装

_Source: https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/ide-deveco-cli-install_

环境准备

DevEco CLI支持在Windows和macOS上运行。

下载和安装DevEco Studio 6.1.0及以上版本的DevEco Studio。

安装Node.js，推荐使用22及以上版本。

安装和更新

npm install -g @deveco/deveco-cli@stable

npm install -g @deveco/deveco-cli

devecocli update

说明

下载和安装命令中的标签@stable是可选项，加上标签@stable表示下载安装稳定版本，不加标签@stable表示下载安装最新版本，可以体验最新功能。

## Code blocks

### Code block 1

```
npm install -g @deveco/deveco-cli@stable
```

### Code block 2

```
npm install -g @deveco/deveco-cli
```

### Code block 3

```
devecocli update
```
